import React, { useContext, useEffect, useState } from "react";
import RideContext from "../../context/ride/rideContext";
import UserContext from "../../context/users/userContext";

const Passengers = () => {
  //const rideContext = useContext(RideContext)
  //const { driverRide  } = rideContext
  //const { passengers } = driverRide

  //const userContext = useContext(UserContext)
  //const { getUser } = userContext

  //const [riders, setRiders] = useState([])

  //useEffect(() => {
  //const getRider = async () => {
  //for (let passenger of passengers) {
  //const user = await getUser(passenger.rider)
  //setRiders(riders => [...riders, user])
  //}
  //}
  //getRider()
  //},[])

  //return (
  //<div>
  //<p className="bodyLg w-med">Passengers</p>
  //{riders && riders.map((rider, index) => {
  //return (
  //<div key={index}>
  //<p>{rider.email}</p>
  //</div>
  //)
  //})}
  //</div>
  //)
  return <></>;
};

export default Passengers;
